.DS_Store

# Renders/video not currently referenced by index.html — keep them
# locally, no need to push unused multi-MB assets to the live site's
# repo. Referenced today: 019, 023, 027 (PNGs), MP4 2K.mp4, and the 3
# hero-*.webp collection photos (now file-referenced, no longer inline
# base64 — see punch list).
images/PLYRA KITCHEN 012.png
images/PLYRA KITCHEN 013.png
images/PLYRA KITCHEN 014.png
images/PLYRA KITCHEN 015.png
images/PLYRA KITCHEN 016.png
images/PLYRA KITCHEN 017.png
images/PLYRA KITCHEN 018.png
images/PLYRA KITCHEN 024.png
images/0.png

# Original full-res furniture/object renders (22-34MB each) — only the
# compressed 1400px web versions (EXP-{4,5,6,7}-web.jpg) are referenced
# by index.html and tracked.
images/EXP 4.png
images/EXP 5.png
images/EXP 6.png
images/EXP 7.png

# Original PICA-PICA renders (21 PNGs, 30-140MB each, plus the source
# MP4/PNG poster) — only the compressed images/pica-pica-*.jpg + the
# copied images/pica-pica-grapes.mp4 are referenced by index.html.
images/PLYRA PRODUCTS/

# Stale duplicate snapshot (old 750-line index.html + original 3 webp
# hero images) and its zip — not the live site, kept for reference
# only, never deployed. The briefs/*.md docs inside it ARE tracked.
plyra_project_1.zip
plyra_project_1/index.html
plyra_project_1/images/

# Duplicate re-exports of the same briefs folder — Marco periodically
# drops a fresh "copy N" here when he updates the master brief
# elsewhere. Each time: diff against plyra_project_1/PLYRA_master_brief.md,
# copy anything new into that tracked folder, then ignore the loose
# copy — never the source of truth once consolidated.
plyra_project_1 copy.zip
plyra_project_1 copy/
plyra_project_1 copy 2.zip
plyra_project_1 copy 2/
plyra_project_1 copy [0-9]*/
plyra_project_1 copy [0-9]*.zip
